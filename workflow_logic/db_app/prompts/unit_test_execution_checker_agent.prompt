Look at the task, and the code produced in response. 
Respond ONLY with:
- 'ALL TESTS PASSED' if all tests passed successfully,
- 'TEST FAILED' if any test failed, suggesting a code error or
- 'TEST CODE ERROR' if the issue is not with the function being checked, but with the unit tests proposed. 